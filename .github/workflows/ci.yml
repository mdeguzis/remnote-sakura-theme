name: CI

on:
  push:
    branches: [main]
  pull_request:

permissions:
  contents: read

jobs:
  verify:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v7
        with:
          node-version-file: .nvmrc
          cache: npm

      - run: npm ci

      - name: Type check
        run: npm run check-types

      - name: Test
        run: npm test

      # The artwork is committed. If a regeneration would change it, the
      # committed files and the generator have drifted apart.
      - name: Artwork is up to date
        run: |
          npm run art
          git diff --exit-code -- assets/ public/logo.png

      - name: Build both zips
        run: npm run build

      - uses: actions/upload-artifact@v4
        with:
          name: sakura-zips
          path: |
            SakuraTheme.zip
            SakuraPlugin.zip
          if-no-files-found: error

  audit:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v7
        with:
          node-version-file: .nvmrc
          cache: npm
      - run: npm ci
      - run: npm audit --audit-level=high
